// Version: 1.0_functions
// Purpose: Practice utilizing functions and loops.
// Comment: I started with an assignment that was to practice functions
and splitting different jobs into those functions. There was also nested
do loops and if conditionals that I was practicing. Eventually I realized
that I can do input validation in its own function so long as the data
types were the same and then return them back to the function it was called
from. I also picked up that I could have a function that gathers user input
and can return them so long as its the same function. Or even functions that
can do a calculation and return the result from the function that was called.


// Version: 2.0_function_arguments
// Purpose: Practice function arguments, char data type, and input validation.
// Comment: I wanted to remember how to use the char data type. I ended up
relearning input validation for a specific data type to make input fullproof.
Then I also experimented with <cctype> to make a users input lowercase using
tolower().

// Version: 3.0_function_arguments
// Purpose: Practice returning values utilizing two arguments in a function
// Comment: I had some practice already utilizing one argument in a function
to perform some logic and return a value. Before I got into arguments passed
by reference I wanted some more practice using two arguments passed through
a function to perform logic and return a value.

// Version: 4.0_reference_arguments
// Purpose: Practice passing arguments through functions utilizing references
// Comment: This was a good refresher on how references are passed for me. I
think this was an area that I struggled with in class. But also since I was 
working on them I realized that the core idea of functions and separating 
logics and jobs in different functions really helped clear up my confusion.
I learned that when arguments are passed through functions they can not be
altered, they are only copies of the original. They can only be used to print 
or calculate logic to be returned. However, functions with arguments passed 
through reference can be altered in the function they are called to and brought 
back to the calling function. There can also be a returned value if wanted.

// Version: 4.1_reference_arguments
// Purpose: More practice updating variables utilizing arguments passed by reference
// Comment: This practice I had to update variables utilizing three reference
arguments. I had one function that had 5 total arguments but only two were not by
reference, those would not be updated, just used for performing logic.

// Version: 4.2_reference_argument
// Purpose: Practice using a function to gather input through variables passed by reference
// Comment: In this one I took the idea of arguments passed by reference to have functions
that were used to gather input from the user and brought back to main to be printed or even
passed through another function thats purpose is to print.

// Version: 4.3_reference_argument
// Purpose: More practice using functions to gather input through varaibles passed by reference
// Comment: I wanted one more practice before I moved on. I made functions that had specific
jobs. Collecting info using arguments passed by reference, validating the integers, and 
printing the information.

// Version: 5.0_for_loop
// Purpose: Practice using a basic for loop
// Comment: This one was pretty easy, I even went above and beyond and used a
separate function to print the output taking an argument of the original user input.

// Version: 5.1_for_loop
// Purpose: Practice using a for loop to find the biggest number
// Comment: This was releatively easy. It was the logic of setting up a variable
largestNum and setting it to the users initial entry vs just to 0. Then putting
it thorugh a loop that takes an input and compares.

// Version: 5.2_for_loop
// Purpose: Practice using a for loop to find the biggest number
// Comment: I just wanted extra practice of this exact problem to make sure that
I had it down in my brain.

// Version: 5.3_for_loop
// Purpose: Practice using a for loop to find even number and odd numbers entered
// Comment: Concept was pretty straight forward utilizing the modulus to see if a number
was even or odd.

// Version: 6.0_nested_for_loops
// Purpose: Practice creating nested for loops
// Comment: I was somewhat familiar of this from class but I definitely thought
that I needed to really get this concept down. I know that moving forward with
arrays I will be utilizing this exact method to navigate them. 
 
// Version: 7.0_arrays
// Purpose: Practice creating and navigating a basic array
// Comment: This one was great to do again. I was relieved to know that I still knew
how to initialize an array and set contents to it using a for loop.

// Version: 7.1_arrays  
// Purpose: More practice creating and navigating an array
// Comment: I essentially wanted to practice navigating arrays again but this time
make it a little more organized with functions. 

// Version: 7.2_arrays
// Purpose: Edit 7.1_arrays.cpp
// Comment: I was told that I can not initialize count from a user input until I can
learn vectors. So in the meantime I replaced count with a constant int SIZE. But the
program is essentially the same, just iterations of how many scores are not influenced
by reading user input.

// Version: 7.3_arrays
// Purpose: Another practice of utilizing an array and finding the highest and lowest values
// Comment: This practice was good. Helped me understand navigating an aray through a
for loop. I was also able to use two reference parameters to update the highest and
lowest values of the numbers added in the array

// Version: 8.0_boolean
// Purpose: Practice the basics of boolean values
// Comment: Found myself genuinely confused at first. I am familiar with conditions
when using if then statements but for some reason putting that into a value had
me a little confused. I ended up completing the program and had it working but
apparently I set it up in an "exploded" format. Some of the code I didn't need.
I'm going to try and clean it up in the next practice.

// Version: 8.1_boolean
// Purpose: More practice on boolean values
// Comment: This time around I was able to complete this in only 6 minutes. I understand now
that I can call the bool function while initializing the bool being used in main. In that 
very function, I don't need to have if then conditionals. I can simply return the called
argument in a condition. If it comes back true then the variable being called and returned 
is both true. Same thing applies if its false. I never knew that before so that was cool. The 
next program I am going to make a more organized and elaborate code to practice this concept.

// Version: 8.2_boolean
// Purpose: Even more practice on boolean values
// Comment: For this practice I combined almost most of what I know up to this point except
for the arrays. I am going to be getting into boolean values with arrays on the next practice.
But I wanted to practice using functions for each input, calculation, and output. I think I did
pretty good on this one. I had a function of getting a number, a function for input validation,
a function for calculating the bool, and a function for printing. All in 7 minutes, I felt good
about this one, and being nice and neat.


// Version:
// Purpose:
// Comment:

// Version:
// Purpose:
// Comment:
